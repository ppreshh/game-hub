# Game Hub

A Game Hub app that allows the user to browse existing games through filters and searches. It uses data from rawg.io

This project for me to learn React through Vite, TypeScript

It uses Chakra UI for styling
